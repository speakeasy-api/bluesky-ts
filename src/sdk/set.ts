/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ozoneSetAddValues } from "../funcs/ozoneSetAddValues.js";
import { ozoneSetDeleteSet } from "../funcs/ozoneSetDeleteSet.js";
import { ozoneSetDeleteValues } from "../funcs/ozoneSetDeleteValues.js";
import { ozoneSetGetValues } from "../funcs/ozoneSetGetValues.js";
import { ozoneSetQuerySets } from "../funcs/ozoneSetQuerySets.js";
import { ozoneSetUpsertSet } from "../funcs/ozoneSetUpsertSet.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class Set extends ClientSDK {
  /**
   * *This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*
   *
   * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
   *
   * Add values to a specific set. Attempting to add values to a set that does not exist will result in an error.
   */
  async addValues(
    request: operations.ToolsOzoneSetAddValuesRequestBody,
    options?: RequestOptions,
  ): Promise<void> {
    return unwrapAsync(ozoneSetAddValues(
      this,
      request,
      options,
    ));
  }

  /**
   * *This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*
   *
   * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
   *
   * Delete an entire set. Attempting to delete a set that does not exist will result in an error.
   */
  async deleteSet(
    request: operations.ToolsOzoneSetDeleteSetRequestBody,
    options?: RequestOptions,
  ): Promise<operations.ToolsOzoneSetDeleteSetResponseBody> {
    return unwrapAsync(ozoneSetDeleteSet(
      this,
      request,
      options,
    ));
  }

  /**
   * *This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*
   *
   * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
   *
   * Delete values from a specific set. Attempting to delete values that are not in the set will not result in an error
   */
  async deleteValues(
    request: operations.ToolsOzoneSetDeleteValuesRequestBody,
    options?: RequestOptions,
  ): Promise<void> {
    return unwrapAsync(ozoneSetDeleteValues(
      this,
      request,
      options,
    ));
  }

  /**
   * *This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*
   *
   * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
   *
   * Get a specific set and its values
   */
  async getValues(
    request: operations.ToolsOzoneSetGetValuesRequest,
    options?: RequestOptions,
  ): Promise<
    PageIterator<operations.ToolsOzoneSetGetValuesResponse, { cursor: string }>
  > {
    return unwrapResultIterator(ozoneSetGetValues(
      this,
      request,
      options,
    ));
  }

  /**
   * *This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*
   *
   * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
   *
   * Query available sets
   */
  async querySets(
    request?: operations.ToolsOzoneSetQuerySetsRequest | undefined,
    options?: RequestOptions,
  ): Promise<
    PageIterator<operations.ToolsOzoneSetQuerySetsResponse, { cursor: string }>
  > {
    return unwrapResultIterator(ozoneSetQuerySets(
      this,
      request,
      options,
    ));
  }

  /**
   * *This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*
   *
   * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
   *
   * Create or update set metadata
   */
  async upsertSet(
    request: components.ToolsOzoneSetDefsSet,
    options?: RequestOptions,
  ): Promise<components.ToolsOzoneSetDefsSetView> {
    return unwrapAsync(ozoneSetUpsertSet(
      this,
      request,
      options,
    ));
  }
}
