/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { setsGetValues } from "../../funcs/setsGetValues.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ToolsOzoneSetGetValuesRequest$inboundSchema,
};

export const tool$setsGetValues: ToolDefinition<typeof args> = {
  name: "sets_get-values",
  description:
    `*This endpoint is part of the [Ozone moderation service](https://ozone.tools/) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the Ozone instance indicated by the DID in the service proxying header. Admin authenentication may also be possible, with request sent directly to the Ozone instance.*

*To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*

Get a specific set and its values

`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await setsGetValues(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    return formatResult(result, apiCall);
  },
};
