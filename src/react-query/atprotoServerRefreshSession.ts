/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { atprotoServerRefreshSession } from "../funcs/atprotoServerRefreshSession.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type AtprotoServerRefreshSessionMutationVariables = {
  options?: RequestOptions;
};

export type AtprotoServerRefreshSessionMutationData =
  operations.ComAtprotoServerRefreshSessionResponse;

/**
 * *This endpoint is part of the atproto PDS server and account management APIs. Requests often require authentication and are made directly to the user's own PDS instance.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Refresh an authentication session. Requires auth using the 'refreshJwt' (not the 'accessJwt').
 */
export function useAtprotoServerRefreshSessionMutation(
  options?: MutationHookOptions<
    AtprotoServerRefreshSessionMutationData,
    Error,
    AtprotoServerRefreshSessionMutationVariables
  >,
): UseMutationResult<
  AtprotoServerRefreshSessionMutationData,
  Error,
  AtprotoServerRefreshSessionMutationVariables
> {
  const client = useBlueskyContext();
  return useMutation({
    ...buildAtprotoServerRefreshSessionMutation(client, options),
    ...options,
  });
}

export function mutationKeyAtprotoServerRefreshSession(): MutationKey {
  return ["@speakeasy-sdks/bluesky", "atprotoServer", "refreshSession"];
}

export function buildAtprotoServerRefreshSessionMutation(
  client$: BlueskyCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: AtprotoServerRefreshSessionMutationVariables,
  ) => Promise<AtprotoServerRefreshSessionMutationData>;
} {
  return {
    mutationKey: mutationKeyAtprotoServerRefreshSession(),
    mutationFn: function atprotoServerRefreshSessionMutationFn({
      options,
    }): Promise<AtprotoServerRefreshSessionMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(atprotoServerRefreshSession(
        client$,
        mergedOptions,
      ));
    },
  };
}
