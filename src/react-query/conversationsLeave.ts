/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { conversationsLeave } from "../funcs/conversationsLeave.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type ConversationsLeaveMutationVariables = {
  request: operations.ChatBskyConvoLeaveConvoRequest;
  options?: RequestOptions;
};

export type ConversationsLeaveMutationData =
  operations.ChatBskyConvoLeaveConvoResponse;

/**
 * *This endpoint is part of the Bluesky Chat (DMs) APIs. Requests usually require authentication, are directed to the user's PDS intance, and proxied to the single central chat service by setting the appropriate service DID (`did:web:api.bsky.chat`) in the service proxying header.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 */
export function useConversationsLeaveMutation(
  options?: MutationHookOptions<
    ConversationsLeaveMutationData,
    Error,
    ConversationsLeaveMutationVariables
  >,
): UseMutationResult<
  ConversationsLeaveMutationData,
  Error,
  ConversationsLeaveMutationVariables
> {
  const client = useBlueskyContext();
  return useMutation({
    ...buildConversationsLeaveMutation(client, options),
    ...options,
  });
}

export function mutationKeyConversationsLeave(): MutationKey {
  return ["@speakeasy-sdks/bluesky", "conversations", "leave"];
}

export function buildConversationsLeaveMutation(
  client$: BlueskyCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: ConversationsLeaveMutationVariables,
  ) => Promise<ConversationsLeaveMutationData>;
} {
  return {
    mutationKey: mutationKeyConversationsLeave(),
    mutationFn: function conversationsLeaveMutationFn({
      request,
      options,
    }): Promise<ConversationsLeaveMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(conversationsLeave(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}
