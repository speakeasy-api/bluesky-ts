/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useQuery,
  UseQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { actorGetProfile } from "../funcs/actorGetProfile.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";
import {
  QueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type ActorGetProfileQueryData =
  components.AppBskyActorDefsProfileViewDetailed;

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get detailed profile view of an actor. Does not require auth, but contains relevant metadata with auth.
 */
export function useActorGetProfile(
  request: operations.AppBskyActorGetProfileRequest,
  options?: QueryHookOptions<components.AppBskyActorDefsProfileViewDetailed>,
): UseQueryResult<components.AppBskyActorDefsProfileViewDetailed, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildActorGetProfileQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get detailed profile view of an actor. Does not require auth, but contains relevant metadata with auth.
 */
export function useActorGetProfileSuspense(
  request: operations.AppBskyActorGetProfileRequest,
  options?: SuspenseQueryHookOptions<
    components.AppBskyActorDefsProfileViewDetailed
  >,
): UseSuspenseQueryResult<
  components.AppBskyActorDefsProfileViewDetailed,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildActorGetProfileQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

export function prefetchActorGetProfile(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request: operations.AppBskyActorGetProfileRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildActorGetProfileQuery(
      client$,
      request,
    ),
  });
}

export function setActorGetProfileData(
  client: QueryClient,
  queryKeyBase: [parameters: { actor: string }],
  data: components.AppBskyActorDefsProfileViewDetailed,
): components.AppBskyActorDefsProfileViewDetailed | undefined {
  const key = queryKeyActorGetProfile(...queryKeyBase);

  return client.setQueryData<components.AppBskyActorDefsProfileViewDetailed>(
    key,
    data,
  );
}

export function invalidateActorGetProfile(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<[parameters: { actor: string }]>,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "actor", "getProfile", ...queryKeyBase],
  });
}

export function invalidateAllActorGetProfile(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "actor", "getProfile"],
  });
}

export function buildActorGetProfileQuery(
  client$: BlueskyCore,
  request: operations.AppBskyActorGetProfileRequest,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<components.AppBskyActorDefsProfileViewDetailed>;
} {
  return {
    retry: false,
    queryKey: queryKeyActorGetProfile({ actor: request.actor }),
    queryFn: async function actorGetProfileQuery(
      ctx,
    ): Promise<components.AppBskyActorDefsProfileViewDetailed> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(actorGetProfile(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function queryKeyActorGetProfile(
  parameters: { actor: string },
): QueryKey {
  return ["bluesky", "actor", "getProfile", parameters];
}
