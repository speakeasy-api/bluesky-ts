/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InfiniteData,
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useInfiniteQuery,
  UseInfiniteQueryResult,
  useQuery,
  UseQueryResult,
  useSuspenseInfiniteQuery,
  UseSuspenseInfiniteQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { reposListMissingBlobs } from "../funcs/reposListMissingBlobs.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";
import { useBlueskyContext } from "./_context.js";
import {
  InfiniteQueryHookOptions,
  QueryHookOptions,
  SuspenseInfiniteQueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type ReposListMissingBlobsQueryData =
  operations.ComAtprotoRepoListMissingBlobsResponse;

export type ReposListMissingBlobsInfiniteQueryData = PageIterator<
  operations.ComAtprotoRepoListMissingBlobsResponse,
  { cursor: string }
>;

export type ReposListMissingBlobsPageParams = PageIterator<
  operations.ComAtprotoRepoListMissingBlobsResponse,
  { cursor: string }
>["~next"];

/**
 * *This endpoint is part of the atproto PDS repository management APIs. Requests usually require authentication (unlike the `com.atproto.sync.*` endpoints), and are made directly to the user's own PDS instance.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Returns a list of missing blobs for the requesting account. Intended to be used in the account migration flow.
 */
export function useReposListMissingBlobs(
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
  options?: QueryHookOptions<ReposListMissingBlobsQueryData>,
): UseQueryResult<ReposListMissingBlobsQueryData, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildReposListMissingBlobsQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the atproto PDS repository management APIs. Requests usually require authentication (unlike the `com.atproto.sync.*` endpoints), and are made directly to the user's own PDS instance.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Returns a list of missing blobs for the requesting account. Intended to be used in the account migration flow.
 */
export function useReposListMissingBlobsSuspense(
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
  options?: SuspenseQueryHookOptions<ReposListMissingBlobsQueryData>,
): UseSuspenseQueryResult<ReposListMissingBlobsQueryData, Error> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildReposListMissingBlobsQuery(
      client,
      request,
      options,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the atproto PDS repository management APIs. Requests usually require authentication (unlike the `com.atproto.sync.*` endpoints), and are made directly to the user's own PDS instance.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Returns a list of missing blobs for the requesting account. Intended to be used in the account migration flow.
 */
export function useReposListMissingBlobsInfinite(
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
  options?: InfiniteQueryHookOptions<ReposListMissingBlobsInfiniteQueryData>,
): UseInfiniteQueryResult<
  InfiniteData<
    ReposListMissingBlobsInfiniteQueryData,
    ReposListMissingBlobsPageParams
  >,
  Error
> {
  const client = useBlueskyContext();
  return useInfiniteQuery<
    ReposListMissingBlobsInfiniteQueryData,
    Error,
    InfiniteData<
      ReposListMissingBlobsInfiniteQueryData,
      ReposListMissingBlobsPageParams
    >,
    QueryKey,
    ReposListMissingBlobsPageParams
  >({
    ...buildReposListMissingBlobsInfiniteQuery(
      client,
      request,
      options,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

/**
 * *This endpoint is part of the atproto PDS repository management APIs. Requests usually require authentication (unlike the `com.atproto.sync.*` endpoints), and are made directly to the user's own PDS instance.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Returns a list of missing blobs for the requesting account. Intended to be used in the account migration flow.
 */
export function useReposListMissingBlobsInfiniteSuspense(
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
  options?: SuspenseInfiniteQueryHookOptions<
    ReposListMissingBlobsInfiniteQueryData
  >,
): UseSuspenseInfiniteQueryResult<
  InfiniteData<
    ReposListMissingBlobsInfiniteQueryData,
    ReposListMissingBlobsPageParams
  >,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseInfiniteQuery<
    ReposListMissingBlobsInfiniteQueryData,
    Error,
    InfiniteData<
      ReposListMissingBlobsInfiniteQueryData,
      ReposListMissingBlobsPageParams
    >,
    QueryKey,
    ReposListMissingBlobsPageParams
  >({
    ...buildReposListMissingBlobsInfiniteQuery(
      client,
      request,
      options,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

export function prefetchReposListMissingBlobs(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildReposListMissingBlobsQuery(
      client$,
      request,
    ),
  });
}

export function setReposListMissingBlobsData(
  client: QueryClient,
  queryKeyBase: [
    parameters: { limit?: number | undefined; cursor?: string | undefined },
  ],
  data: ReposListMissingBlobsQueryData,
): ReposListMissingBlobsQueryData | undefined {
  const key = queryKeyReposListMissingBlobs(...queryKeyBase);

  return client.setQueryData<ReposListMissingBlobsQueryData>(key, data);
}

export function invalidateReposListMissingBlobs(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: { limit?: number | undefined; cursor?: string | undefined }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: [
      "@speakeasy-api/bluesky",
      "repos",
      "listMissingBlobs",
      ...queryKeyBase,
    ],
  });
}

export function invalidateAllReposListMissingBlobs(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["@speakeasy-api/bluesky", "repos", "listMissingBlobs"],
  });
}

export function buildReposListMissingBlobsQuery(
  client$: BlueskyCore,
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<ReposListMissingBlobsQueryData>;
} {
  return {
    queryKey: queryKeyReposListMissingBlobs({
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function reposListMissingBlobsQueryFn(
      ctx,
    ): Promise<ReposListMissingBlobsQueryData> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(reposListMissingBlobs(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function buildReposListMissingBlobsInfiniteQuery(
  client$: BlueskyCore,
  request?: operations.ComAtprotoRepoListMissingBlobsRequest | undefined,
  options?: RequestOptions,
): {
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext<QueryKey, ReposListMissingBlobsPageParams>,
  ) => Promise<ReposListMissingBlobsInfiniteQueryData>;
} {
  return {
    queryKey: queryKeyReposListMissingBlobs({
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function reposListMissingBlobsQuery(
      ctx,
    ): Promise<ReposListMissingBlobsInfiniteQueryData> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      if (!ctx.pageParam) {
        return unwrapResultIterator(reposListMissingBlobs(
          client$,
          request,
          mergedOptions,
        ));
      }

      return unwrapResultIterator(reposListMissingBlobs(
        client$,
        {
          ...request,
          cursor: ctx.pageParam.cursor,
        },
        mergedOptions,
      ));
    },
  };
}

export function queryKeyReposListMissingBlobs(
  parameters: { limit?: number | undefined; cursor?: string | undefined },
): QueryKey {
  return ["@speakeasy-api/bluesky", "repos", "listMissingBlobs", parameters];
}
