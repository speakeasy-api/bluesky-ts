/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InfiniteData,
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useInfiniteQuery,
  UseInfiniteQueryResult,
  useQuery,
  UseQueryResult,
  useSuspenseInfiniteQuery,
  UseSuspenseInfiniteQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { atprotoSyncListRepos } from "../funcs/atprotoSyncListRepos.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";
import { useBlueskyContext } from "./_context.js";
import {
  InfiniteQueryHookOptions,
  QueryHookOptions,
  SuspenseInfiniteQueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type AtprotoSyncListReposQueryData = PageIterator<
  operations.ComAtprotoSyncListReposResponse,
  { cursor: string }
>;
export type AtprotoSyncListReposPageParams = PageIterator<
  operations.ComAtprotoSyncListReposResponse,
  { cursor: string }
>["~next"];

/**
 * This endpoint is part of the atproto repository synchronization APIs. Requests usually do not require authentication, and can be made to PDS intances or Relay instances.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates all the DID, rev, and commit CID for all repos hosted by this service. Does not require auth; implemented by PDS and Relay.
 */
export function useAtprotoSyncListRepos(
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
  options?: QueryHookOptions<operations.ComAtprotoSyncListReposResponse>,
): UseQueryResult<operations.ComAtprotoSyncListReposResponse, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildAtprotoSyncListReposQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * This endpoint is part of the atproto repository synchronization APIs. Requests usually do not require authentication, and can be made to PDS intances or Relay instances.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates all the DID, rev, and commit CID for all repos hosted by this service. Does not require auth; implemented by PDS and Relay.
 */
export function useAtprotoSyncListReposSuspense(
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
  options?: SuspenseQueryHookOptions<
    operations.ComAtprotoSyncListReposResponse
  >,
): UseSuspenseQueryResult<operations.ComAtprotoSyncListReposResponse, Error> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildAtprotoSyncListReposQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * This endpoint is part of the atproto repository synchronization APIs. Requests usually do not require authentication, and can be made to PDS intances or Relay instances.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates all the DID, rev, and commit CID for all repos hosted by this service. Does not require auth; implemented by PDS and Relay.
 */
export function useAtprotoSyncListReposInfinite(
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
  options?: InfiniteQueryHookOptions<
    PageIterator<operations.ComAtprotoSyncListReposResponse, { cursor: string }>
  >,
): UseInfiniteQueryResult<
  InfiniteData<AtprotoSyncListReposQueryData, AtprotoSyncListReposPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useInfiniteQuery<
    AtprotoSyncListReposQueryData,
    Error,
    InfiniteData<AtprotoSyncListReposQueryData, AtprotoSyncListReposPageParams>,
    QueryKey,
    AtprotoSyncListReposPageParams
  >({
    ...buildAtprotoSyncListReposInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

/**
 * This endpoint is part of the atproto repository synchronization APIs. Requests usually do not require authentication, and can be made to PDS intances or Relay instances.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates all the DID, rev, and commit CID for all repos hosted by this service. Does not require auth; implemented by PDS and Relay.
 */
export function useAtprotoSyncListReposInfiniteSuspense(
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
  options?: SuspenseInfiniteQueryHookOptions<
    PageIterator<operations.ComAtprotoSyncListReposResponse, { cursor: string }>
  >,
): UseSuspenseInfiniteQueryResult<
  InfiniteData<AtprotoSyncListReposQueryData, AtprotoSyncListReposPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseInfiniteQuery<
    AtprotoSyncListReposQueryData,
    Error,
    InfiniteData<AtprotoSyncListReposQueryData, AtprotoSyncListReposPageParams>,
    QueryKey,
    AtprotoSyncListReposPageParams
  >({
    ...buildAtprotoSyncListReposInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

export function prefetchAtprotoSyncListRepos(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildAtprotoSyncListReposQuery(
      client$,
      request,
    ),
  });
}

export function setAtprotoSyncListReposData(
  client: QueryClient,
  queryKeyBase: [
    parameters: { limit?: number | undefined; cursor?: string | undefined },
  ],
  data: operations.ComAtprotoSyncListReposResponse,
): operations.ComAtprotoSyncListReposResponse | undefined {
  const key = queryKeyAtprotoSyncListRepos(...queryKeyBase);

  return client.setQueryData<operations.ComAtprotoSyncListReposResponse>(
    key,
    data,
  );
}

export function invalidateAtprotoSyncListRepos(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: { limit?: number | undefined; cursor?: string | undefined }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "sync", "listRepos", ...queryKeyBase],
  });
}

export function invalidateAllAtprotoSyncListRepos(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "sync", "listRepos"],
  });
}

export function buildAtprotoSyncListReposQuery(
  client$: BlueskyCore,
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<operations.ComAtprotoSyncListReposResponse>;
} {
  return {
    retry: false,
    queryKey: queryKeyAtprotoSyncListRepos({
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function atprotoSyncListReposQuery(
      ctx,
    ): Promise<operations.ComAtprotoSyncListReposResponse> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(atprotoSyncListRepos(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function buildAtprotoSyncListReposInfiniteQuery(
  client$: BlueskyCore,
  request?: operations.ComAtprotoSyncListReposRequest | undefined,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext<QueryKey, AtprotoSyncListReposPageParams>,
  ) => Promise<
    PageIterator<operations.ComAtprotoSyncListReposResponse, { cursor: string }>
  >;
} {
  return {
    retry: false,
    queryKey: queryKeyAtprotoSyncListRepos({
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function atprotoSyncListReposQuery(
      ctx,
    ): Promise<
      PageIterator<
        operations.ComAtprotoSyncListReposResponse,
        { cursor: string }
      >
    > {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      if (!ctx.pageParam) {
        return unwrapResultIterator(atprotoSyncListRepos(
          client$,
          request,
          mergedOptions,
        ));
      }

      return unwrapResultIterator(atprotoSyncListRepos(
        client$,
        {
          ...request,
          cursor: ctx.pageParam.cursor,
        },
        mergedOptions,
      ));
    },
  };
}

export function queryKeyAtprotoSyncListRepos(
  parameters: { limit?: number | undefined; cursor?: string | undefined },
): QueryKey {
  return ["bluesky", "sync", "listRepos", parameters];
}
