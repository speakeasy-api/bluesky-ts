/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationOptions,
  UseMutationResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { atprotoServerReserveSigningKey } from "../funcs/atprotoServerReserveSigningKey.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";

export type AtprotoServerReserveSigningKeyMutationVariables = {
  request?: operations.ComAtprotoServerReserveSigningKeyRequestBody | undefined;
  options?: RequestOptions;
};

/**
 * *This endpoint is part of the atproto PDS server and account management APIs. Requests often require authentication and are made directly to the user's own PDS instance.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Reserve a repo signing key, for use with account creation. Necessary so that a DID PLC update operation can be constructed during an account migraiton. Public and does not require auth; implemented by PDS. NOTE: this endpoint may change when full account migration is implemented.
 */
export function useAtprotoServerReserveSigningKeyMutation(
  options?: UseMutationOptions<
    operations.ComAtprotoServerReserveSigningKeyResponseBody,
    Error,
    AtprotoServerReserveSigningKeyMutationVariables
  >,
): UseMutationResult<
  operations.ComAtprotoServerReserveSigningKeyResponseBody,
  Error,
  AtprotoServerReserveSigningKeyMutationVariables
> {
  const client = useBlueskyContext();
  return useMutation({
    ...buildAtprotoServerReserveSigningKeyMutation(client),
    ...options,
  });
}

export function mutationKeyAtprotoServerReserveSigningKey(): MutationKey {
  return ["bluesky", "server", "reserveSigningKey"];
}

export function buildAtprotoServerReserveSigningKeyMutation(
  client$: BlueskyCore,
): {
  retry: boolean;
  mutationKey: MutationKey;
  mutationFn: (
    variables: AtprotoServerReserveSigningKeyMutationVariables,
  ) => Promise<operations.ComAtprotoServerReserveSigningKeyResponseBody>;
} {
  return {
    retry: false,
    mutationKey: mutationKeyAtprotoServerReserveSigningKey(),
    mutationFn: function atprotoServerReserveSigningKeyQuery({
      request,
      options,
    }): Promise<operations.ComAtprotoServerReserveSigningKeyResponseBody> {
      return unwrapAsync(atprotoServerReserveSigningKey(
        client$,
        request,
        options,
      ));
    },
  };
}
