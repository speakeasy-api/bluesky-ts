/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationOptions,
  UseMutationResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { graphMuteThread } from "../funcs/graphMuteThread.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";

export type GraphMuteThreadMutationVariables = {
  request: operations.AppBskyGraphMuteThreadRequestBody;
  options?: RequestOptions;
};

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Mutes a thread preventing notifications from the thread and any of its children. Mutes are private in Bluesky. Requires auth.
 */
export function useGraphMuteThreadMutation(
  options?: UseMutationOptions<void, Error, GraphMuteThreadMutationVariables>,
): UseMutationResult<void, Error, GraphMuteThreadMutationVariables> {
  const client = useBlueskyContext();
  return useMutation({
    ...buildGraphMuteThreadMutation(client),
    ...options,
  });
}

export function mutationKeyGraphMuteThread(): MutationKey {
  return ["bluesky", "graph", "muteThread"];
}

export function buildGraphMuteThreadMutation(
  client$: BlueskyCore,
): {
  retry: boolean;
  mutationKey: MutationKey;
  mutationFn: (variables: GraphMuteThreadMutationVariables) => Promise<void>;
} {
  return {
    retry: false,
    mutationKey: mutationKeyGraphMuteThread(),
    mutationFn: function graphMuteThreadQuery({
      request,
      options,
    }): Promise<void> {
      return unwrapAsync(graphMuteThread(
        client$,
        request,
        options,
      ));
    },
  };
}
