/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationOptions,
  UseMutationResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { notificationRegisterPush } from "../funcs/notificationRegisterPush.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";

export type NotificationRegisterPushMutationVariables = {
  request: operations.AppBskyNotificationRegisterPushRequestBody;
  options?: RequestOptions;
};

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Register to receive push notifications, via a specified service, for the requesting account. Requires auth.
 */
export function useNotificationRegisterPushMutation(
  options?: UseMutationOptions<
    void,
    Error,
    NotificationRegisterPushMutationVariables
  >,
): UseMutationResult<void, Error, NotificationRegisterPushMutationVariables> {
  const client = useBlueskyContext();
  return useMutation({
    ...buildNotificationRegisterPushMutation(client),
    ...options,
  });
}

export function mutationKeyNotificationRegisterPush(): MutationKey {
  return ["bluesky", "notification", "registerPush"];
}

export function buildNotificationRegisterPushMutation(
  client$: BlueskyCore,
): {
  retry: boolean;
  mutationKey: MutationKey;
  mutationFn: (
    variables: NotificationRegisterPushMutationVariables,
  ) => Promise<void>;
} {
  return {
    retry: false,
    mutationKey: mutationKeyNotificationRegisterPush(),
    mutationFn: function notificationRegisterPushQuery({
      request,
      options,
    }): Promise<void> {
      return unwrapAsync(notificationRegisterPush(
        client$,
        request,
        options,
      ));
    },
  };
}
