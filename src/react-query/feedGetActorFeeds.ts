/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InfiniteData,
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useInfiniteQuery,
  UseInfiniteQueryResult,
  useQuery,
  UseQueryResult,
  useSuspenseInfiniteQuery,
  UseSuspenseInfiniteQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { feedGetActorFeeds } from "../funcs/feedGetActorFeeds.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";
import { useBlueskyContext } from "./_context.js";
import {
  InfiniteQueryHookOptions,
  QueryHookOptions,
  SuspenseInfiniteQueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type FeedGetActorFeedsQueryData = PageIterator<
  operations.AppBskyFeedGetActorFeedsResponse,
  { cursor: string }
>;
export type FeedGetActorFeedsPageParams = PageIterator<
  operations.AppBskyFeedGetActorFeedsResponse,
  { cursor: string }
>["~next"];

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a list of feeds (feed generator records) created by the actor (in the actor's repo).
 */
export function useFeedGetActorFeeds(
  request: operations.AppBskyFeedGetActorFeedsRequest,
  options?: QueryHookOptions<operations.AppBskyFeedGetActorFeedsResponse>,
): UseQueryResult<operations.AppBskyFeedGetActorFeedsResponse, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildFeedGetActorFeedsQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a list of feeds (feed generator records) created by the actor (in the actor's repo).
 */
export function useFeedGetActorFeedsSuspense(
  request: operations.AppBskyFeedGetActorFeedsRequest,
  options?: SuspenseQueryHookOptions<
    operations.AppBskyFeedGetActorFeedsResponse
  >,
): UseSuspenseQueryResult<operations.AppBskyFeedGetActorFeedsResponse, Error> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildFeedGetActorFeedsQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a list of feeds (feed generator records) created by the actor (in the actor's repo).
 */
export function useFeedGetActorFeedsInfinite(
  request: operations.AppBskyFeedGetActorFeedsRequest,
  options?: InfiniteQueryHookOptions<
    PageIterator<
      operations.AppBskyFeedGetActorFeedsResponse,
      { cursor: string }
    >
  >,
): UseInfiniteQueryResult<
  InfiniteData<FeedGetActorFeedsQueryData, FeedGetActorFeedsPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useInfiniteQuery<
    FeedGetActorFeedsQueryData,
    Error,
    InfiniteData<FeedGetActorFeedsQueryData, FeedGetActorFeedsPageParams>,
    QueryKey,
    FeedGetActorFeedsPageParams
  >({
    ...buildFeedGetActorFeedsInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a list of feeds (feed generator records) created by the actor (in the actor's repo).
 */
export function useFeedGetActorFeedsInfiniteSuspense(
  request: operations.AppBskyFeedGetActorFeedsRequest,
  options?: SuspenseInfiniteQueryHookOptions<
    PageIterator<
      operations.AppBskyFeedGetActorFeedsResponse,
      { cursor: string }
    >
  >,
): UseSuspenseInfiniteQueryResult<
  InfiniteData<FeedGetActorFeedsQueryData, FeedGetActorFeedsPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseInfiniteQuery<
    FeedGetActorFeedsQueryData,
    Error,
    InfiniteData<FeedGetActorFeedsQueryData, FeedGetActorFeedsPageParams>,
    QueryKey,
    FeedGetActorFeedsPageParams
  >({
    ...buildFeedGetActorFeedsInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

export function prefetchFeedGetActorFeeds(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request: operations.AppBskyFeedGetActorFeedsRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildFeedGetActorFeedsQuery(
      client$,
      request,
    ),
  });
}

export function setFeedGetActorFeedsData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      actor: string;
      limit?: number | undefined;
      cursor?: string | undefined;
    },
  ],
  data: operations.AppBskyFeedGetActorFeedsResponse,
): operations.AppBskyFeedGetActorFeedsResponse | undefined {
  const key = queryKeyFeedGetActorFeeds(...queryKeyBase);

  return client.setQueryData<operations.AppBskyFeedGetActorFeedsResponse>(
    key,
    data,
  );
}

export function invalidateFeedGetActorFeeds(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      actor: string;
      limit?: number | undefined;
      cursor?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "feed", "getActorFeeds", ...queryKeyBase],
  });
}

export function invalidateAllFeedGetActorFeeds(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "feed", "getActorFeeds"],
  });
}

export function buildFeedGetActorFeedsQuery(
  client$: BlueskyCore,
  request: operations.AppBskyFeedGetActorFeedsRequest,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<operations.AppBskyFeedGetActorFeedsResponse>;
} {
  return {
    retry: false,
    queryKey: queryKeyFeedGetActorFeeds({
      actor: request.actor,
      limit: request.limit,
      cursor: request.cursor,
    }),
    queryFn: async function feedGetActorFeedsQuery(
      ctx,
    ): Promise<operations.AppBskyFeedGetActorFeedsResponse> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(feedGetActorFeeds(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function buildFeedGetActorFeedsInfiniteQuery(
  client$: BlueskyCore,
  request: operations.AppBskyFeedGetActorFeedsRequest,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext<QueryKey, FeedGetActorFeedsPageParams>,
  ) => Promise<
    PageIterator<
      operations.AppBskyFeedGetActorFeedsResponse,
      { cursor: string }
    >
  >;
} {
  return {
    retry: false,
    queryKey: queryKeyFeedGetActorFeeds({
      actor: request.actor,
      limit: request.limit,
      cursor: request.cursor,
    }),
    queryFn: async function feedGetActorFeedsQuery(
      ctx,
    ): Promise<
      PageIterator<
        operations.AppBskyFeedGetActorFeedsResponse,
        { cursor: string }
      >
    > {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      if (!ctx.pageParam) {
        return unwrapResultIterator(feedGetActorFeeds(
          client$,
          request,
          mergedOptions,
        ));
      }

      return unwrapResultIterator(feedGetActorFeeds(
        client$,
        {
          ...request,
          cursor: ctx.pageParam.cursor,
        },
        mergedOptions,
      ));
    },
  };
}

export function queryKeyFeedGetActorFeeds(
  parameters: {
    actor: string;
    limit?: number | undefined;
    cursor?: string | undefined;
  },
): QueryKey {
  return ["bluesky", "feed", "getActorFeeds", parameters];
}
