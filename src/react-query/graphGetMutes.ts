/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InfiniteData,
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useInfiniteQuery,
  UseInfiniteQueryResult,
  useQuery,
  UseQueryResult,
  useSuspenseInfiniteQuery,
  UseSuspenseInfiniteQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { graphGetMutes } from "../funcs/graphGetMutes.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";
import { useBlueskyContext } from "./_context.js";
import {
  InfiniteQueryHookOptions,
  QueryHookOptions,
  SuspenseInfiniteQueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type GraphGetMutesQueryData = PageIterator<
  operations.AppBskyGraphGetMutesResponse,
  { cursor: string }
>;
export type GraphGetMutesPageParams = PageIterator<
  operations.AppBskyGraphGetMutesResponse,
  { cursor: string }
>["~next"];

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates accounts that the requesting account (actor) currently has muted. Requires auth.
 */
export function useGraphGetMutes(
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
  options?: QueryHookOptions<operations.AppBskyGraphGetMutesResponse>,
): UseQueryResult<operations.AppBskyGraphGetMutesResponse, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildGraphGetMutesQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates accounts that the requesting account (actor) currently has muted. Requires auth.
 */
export function useGraphGetMutesSuspense(
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
  options?: SuspenseQueryHookOptions<operations.AppBskyGraphGetMutesResponse>,
): UseSuspenseQueryResult<operations.AppBskyGraphGetMutesResponse, Error> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildGraphGetMutesQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates accounts that the requesting account (actor) currently has muted. Requires auth.
 */
export function useGraphGetMutesInfinite(
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
  options?: InfiniteQueryHookOptions<
    PageIterator<operations.AppBskyGraphGetMutesResponse, { cursor: string }>
  >,
): UseInfiniteQueryResult<
  InfiniteData<GraphGetMutesQueryData, GraphGetMutesPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useInfiniteQuery<
    GraphGetMutesQueryData,
    Error,
    InfiniteData<GraphGetMutesQueryData, GraphGetMutesPageParams>,
    QueryKey,
    GraphGetMutesPageParams
  >({
    ...buildGraphGetMutesInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Enumerates accounts that the requesting account (actor) currently has muted. Requires auth.
 */
export function useGraphGetMutesInfiniteSuspense(
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
  options?: SuspenseInfiniteQueryHookOptions<
    PageIterator<operations.AppBskyGraphGetMutesResponse, { cursor: string }>
  >,
): UseSuspenseInfiniteQueryResult<
  InfiniteData<GraphGetMutesQueryData, GraphGetMutesPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseInfiniteQuery<
    GraphGetMutesQueryData,
    Error,
    InfiniteData<GraphGetMutesQueryData, GraphGetMutesPageParams>,
    QueryKey,
    GraphGetMutesPageParams
  >({
    ...buildGraphGetMutesInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

export function prefetchGraphGetMutes(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildGraphGetMutesQuery(
      client$,
      request,
    ),
  });
}

export function setGraphGetMutesData(
  client: QueryClient,
  queryKeyBase: [
    parameters: { limit?: number | undefined; cursor?: string | undefined },
  ],
  data: operations.AppBskyGraphGetMutesResponse,
): operations.AppBskyGraphGetMutesResponse | undefined {
  const key = queryKeyGraphGetMutes(...queryKeyBase);

  return client.setQueryData<operations.AppBskyGraphGetMutesResponse>(
    key,
    data,
  );
}

export function invalidateGraphGetMutes(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: { limit?: number | undefined; cursor?: string | undefined }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "graph", "getMutes", ...queryKeyBase],
  });
}

export function invalidateAllGraphGetMutes(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "graph", "getMutes"],
  });
}

export function buildGraphGetMutesQuery(
  client$: BlueskyCore,
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<operations.AppBskyGraphGetMutesResponse>;
} {
  return {
    retry: false,
    queryKey: queryKeyGraphGetMutes({
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function graphGetMutesQuery(
      ctx,
    ): Promise<operations.AppBskyGraphGetMutesResponse> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(graphGetMutes(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function buildGraphGetMutesInfiniteQuery(
  client$: BlueskyCore,
  request?: operations.AppBskyGraphGetMutesRequest | undefined,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext<QueryKey, GraphGetMutesPageParams>,
  ) => Promise<
    PageIterator<operations.AppBskyGraphGetMutesResponse, { cursor: string }>
  >;
} {
  return {
    retry: false,
    queryKey: queryKeyGraphGetMutes({
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function graphGetMutesQuery(
      ctx,
    ): Promise<
      PageIterator<operations.AppBskyGraphGetMutesResponse, { cursor: string }>
    > {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      if (!ctx.pageParam) {
        return unwrapResultIterator(graphGetMutes(
          client$,
          request,
          mergedOptions,
        ));
      }

      return unwrapResultIterator(graphGetMutes(
        client$,
        {
          ...request,
          cursor: ctx.pageParam.cursor,
        },
        mergedOptions,
      ));
    },
  };
}

export function queryKeyGraphGetMutes(
  parameters: { limit?: number | undefined; cursor?: string | undefined },
): QueryKey {
  return ["bluesky", "graph", "getMutes", parameters];
}
