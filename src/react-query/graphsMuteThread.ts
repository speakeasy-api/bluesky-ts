/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { graphsMuteThread } from "../funcs/graphsMuteThread.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useBlueskyContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type GraphsMuteThreadMutationVariables = {
  request: operations.AppBskyGraphMuteThreadRequestBody;
  options?: RequestOptions;
};

export type GraphsMuteThreadMutationData = void;

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Mutes a thread preventing notifications from the thread and any of its children. Mutes are private in Bluesky. Requires auth.
 */
export function useGraphsMuteThreadMutation(
  options?: MutationHookOptions<
    GraphsMuteThreadMutationData,
    Error,
    GraphsMuteThreadMutationVariables
  >,
): UseMutationResult<
  GraphsMuteThreadMutationData,
  Error,
  GraphsMuteThreadMutationVariables
> {
  const client = useBlueskyContext();
  return useMutation({
    ...buildGraphsMuteThreadMutation(client, options),
    ...options,
  });
}

export function mutationKeyGraphsMuteThread(): MutationKey {
  return ["@speakeasy-api/bluesky", "graphs", "muteThread"];
}

export function buildGraphsMuteThreadMutation(
  client$: BlueskyCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: GraphsMuteThreadMutationVariables,
  ) => Promise<GraphsMuteThreadMutationData>;
} {
  return {
    mutationKey: mutationKeyGraphsMuteThread(),
    mutationFn: function graphsMuteThreadMutationFn({
      request,
      options,
    }): Promise<GraphsMuteThreadMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(graphsMuteThread(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}
