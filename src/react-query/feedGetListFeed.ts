/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InfiniteData,
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useInfiniteQuery,
  UseInfiniteQueryResult,
  useQuery,
  UseQueryResult,
  useSuspenseInfiniteQuery,
  UseSuspenseInfiniteQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { feedGetListFeed } from "../funcs/feedGetListFeed.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";
import { useBlueskyContext } from "./_context.js";
import {
  InfiniteQueryHookOptions,
  QueryHookOptions,
  SuspenseInfiniteQueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type FeedGetListFeedQueryData = PageIterator<
  operations.AppBskyFeedGetListFeedResponse,
  { cursor: string }
>;
export type FeedGetListFeedPageParams = PageIterator<
  operations.AppBskyFeedGetListFeedResponse,
  { cursor: string }
>["~next"];

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a feed of recent posts from a list (posts and reposts from any actors on the list). Does not require auth.
 */
export function useFeedGetListFeed(
  request: operations.AppBskyFeedGetListFeedRequest,
  options?: QueryHookOptions<operations.AppBskyFeedGetListFeedResponse>,
): UseQueryResult<operations.AppBskyFeedGetListFeedResponse, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildFeedGetListFeedQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a feed of recent posts from a list (posts and reposts from any actors on the list). Does not require auth.
 */
export function useFeedGetListFeedSuspense(
  request: operations.AppBskyFeedGetListFeedRequest,
  options?: SuspenseQueryHookOptions<operations.AppBskyFeedGetListFeedResponse>,
): UseSuspenseQueryResult<operations.AppBskyFeedGetListFeedResponse, Error> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildFeedGetListFeedQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a feed of recent posts from a list (posts and reposts from any actors on the list). Does not require auth.
 */
export function useFeedGetListFeedInfinite(
  request: operations.AppBskyFeedGetListFeedRequest,
  options?: InfiniteQueryHookOptions<
    PageIterator<operations.AppBskyFeedGetListFeedResponse, { cursor: string }>
  >,
): UseInfiniteQueryResult<
  InfiniteData<FeedGetListFeedQueryData, FeedGetListFeedPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useInfiniteQuery<
    FeedGetListFeedQueryData,
    Error,
    InfiniteData<FeedGetListFeedQueryData, FeedGetListFeedPageParams>,
    QueryKey,
    FeedGetListFeedPageParams
  >({
    ...buildFeedGetListFeedInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Get a feed of recent posts from a list (posts and reposts from any actors on the list). Does not require auth.
 */
export function useFeedGetListFeedInfiniteSuspense(
  request: operations.AppBskyFeedGetListFeedRequest,
  options?: SuspenseInfiniteQueryHookOptions<
    PageIterator<operations.AppBskyFeedGetListFeedResponse, { cursor: string }>
  >,
): UseSuspenseInfiniteQueryResult<
  InfiniteData<FeedGetListFeedQueryData, FeedGetListFeedPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseInfiniteQuery<
    FeedGetListFeedQueryData,
    Error,
    InfiniteData<FeedGetListFeedQueryData, FeedGetListFeedPageParams>,
    QueryKey,
    FeedGetListFeedPageParams
  >({
    ...buildFeedGetListFeedInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

export function prefetchFeedGetListFeed(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request: operations.AppBskyFeedGetListFeedRequest,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildFeedGetListFeedQuery(
      client$,
      request,
    ),
  });
}

export function setFeedGetListFeedData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      list: string;
      limit?: number | undefined;
      cursor?: string | undefined;
    },
  ],
  data: operations.AppBskyFeedGetListFeedResponse,
): operations.AppBskyFeedGetListFeedResponse | undefined {
  const key = queryKeyFeedGetListFeed(...queryKeyBase);

  return client.setQueryData<operations.AppBskyFeedGetListFeedResponse>(
    key,
    data,
  );
}

export function invalidateFeedGetListFeed(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      list: string;
      limit?: number | undefined;
      cursor?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "feed", "getListFeed", ...queryKeyBase],
  });
}

export function invalidateAllFeedGetListFeed(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "feed", "getListFeed"],
  });
}

export function buildFeedGetListFeedQuery(
  client$: BlueskyCore,
  request: operations.AppBskyFeedGetListFeedRequest,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<operations.AppBskyFeedGetListFeedResponse>;
} {
  return {
    retry: false,
    queryKey: queryKeyFeedGetListFeed({
      list: request.list,
      limit: request.limit,
      cursor: request.cursor,
    }),
    queryFn: async function feedGetListFeedQuery(
      ctx,
    ): Promise<operations.AppBskyFeedGetListFeedResponse> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(feedGetListFeed(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function buildFeedGetListFeedInfiniteQuery(
  client$: BlueskyCore,
  request: operations.AppBskyFeedGetListFeedRequest,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext<QueryKey, FeedGetListFeedPageParams>,
  ) => Promise<
    PageIterator<operations.AppBskyFeedGetListFeedResponse, { cursor: string }>
  >;
} {
  return {
    retry: false,
    queryKey: queryKeyFeedGetListFeed({
      list: request.list,
      limit: request.limit,
      cursor: request.cursor,
    }),
    queryFn: async function feedGetListFeedQuery(
      ctx,
    ): Promise<
      PageIterator<
        operations.AppBskyFeedGetListFeedResponse,
        { cursor: string }
      >
    > {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      if (!ctx.pageParam) {
        return unwrapResultIterator(feedGetListFeed(
          client$,
          request,
          mergedOptions,
        ));
      }

      return unwrapResultIterator(feedGetListFeed(
        client$,
        {
          ...request,
          cursor: ctx.pageParam.cursor,
        },
        mergedOptions,
      ));
    },
  };
}

export function queryKeyFeedGetListFeed(
  parameters: {
    list: string;
    limit?: number | undefined;
    cursor?: string | undefined;
  },
): QueryKey {
  return ["bluesky", "feed", "getListFeed", parameters];
}
