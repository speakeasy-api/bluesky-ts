/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  InfiniteData,
  InvalidateQueryFilters,
  QueryClient,
  QueryFunctionContext,
  QueryKey,
  useInfiniteQuery,
  UseInfiniteQueryResult,
  useQuery,
  UseQueryResult,
  useSuspenseInfiniteQuery,
  UseSuspenseInfiniteQueryResult,
  useSuspenseQuery,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { BlueskyCore } from "../core.js";
import { actorSearchActors } from "../funcs/actorSearchActors.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";
import { useBlueskyContext } from "./_context.js";
import {
  InfiniteQueryHookOptions,
  QueryHookOptions,
  SuspenseInfiniteQueryHookOptions,
  SuspenseQueryHookOptions,
  TupleToPrefixes,
} from "./_types.js";

export type ActorSearchActorsQueryData = PageIterator<
  operations.AppBskyActorSearchActorsResponse,
  { cursor: string }
>;
export type ActorSearchActorsPageParams = PageIterator<
  operations.AppBskyActorSearchActorsResponse,
  { cursor: string }
>["~next"];

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Find actors (profiles) matching search criteria. Does not require auth.
 */
export function useActorSearchActors(
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
  options?: QueryHookOptions<operations.AppBskyActorSearchActorsResponse>,
): UseQueryResult<operations.AppBskyActorSearchActorsResponse, Error> {
  const client = useBlueskyContext();
  return useQuery({
    ...buildActorSearchActorsQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Find actors (profiles) matching search criteria. Does not require auth.
 */
export function useActorSearchActorsSuspense(
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
  options?: SuspenseQueryHookOptions<
    operations.AppBskyActorSearchActorsResponse
  >,
): UseSuspenseQueryResult<operations.AppBskyActorSearchActorsResponse, Error> {
  const client = useBlueskyContext();
  return useSuspenseQuery({
    ...buildActorSearchActorsQuery(
      client,
      request,
      options?.request,
    ),
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Find actors (profiles) matching search criteria. Does not require auth.
 */
export function useActorSearchActorsInfinite(
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
  options?: InfiniteQueryHookOptions<
    PageIterator<
      operations.AppBskyActorSearchActorsResponse,
      { cursor: string }
    >
  >,
): UseInfiniteQueryResult<
  InfiniteData<ActorSearchActorsQueryData, ActorSearchActorsPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useInfiniteQuery<
    ActorSearchActorsQueryData,
    Error,
    InfiniteData<ActorSearchActorsQueryData, ActorSearchActorsPageParams>,
    QueryKey,
    ActorSearchActorsPageParams
  >({
    ...buildActorSearchActorsInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

/**
 * *This endpoint is part of the Bluesky application Lexicon APIs (`app.bsky.*`). Public endpoints which don't require authentication can be made directly against the public Bluesky AppView API: https://public.api.bsky.app. Authenticated requests are usually made to the user's PDS, with automatic service proxying. Authenticated requests can be used for both public and non-public endpoints.*
 *
 * *To learn more about calling atproto API endpoints like this one, see the [API Hosts and Auth](/docs/advanced-guides/api-directory) guide.*
 *
 * Find actors (profiles) matching search criteria. Does not require auth.
 */
export function useActorSearchActorsInfiniteSuspense(
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
  options?: SuspenseInfiniteQueryHookOptions<
    PageIterator<
      operations.AppBskyActorSearchActorsResponse,
      { cursor: string }
    >
  >,
): UseSuspenseInfiniteQueryResult<
  InfiniteData<ActorSearchActorsQueryData, ActorSearchActorsPageParams>,
  Error
> {
  const client = useBlueskyContext();
  return useSuspenseInfiniteQuery<
    ActorSearchActorsQueryData,
    Error,
    InfiniteData<ActorSearchActorsQueryData, ActorSearchActorsPageParams>,
    QueryKey,
    ActorSearchActorsPageParams
  >({
    ...buildActorSearchActorsInfiniteQuery(
      client,
      request,
      options?.request,
    ),
    initialPageParam: options?.initialPageParam,
    getNextPageParam: (previousPage) => previousPage["~next"],
    ...options,
  });
}

export function prefetchActorSearchActors(
  queryClient: QueryClient,
  client$: BlueskyCore,
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
): Promise<void> {
  return queryClient.prefetchQuery({
    ...buildActorSearchActorsQuery(
      client$,
      request,
    ),
  });
}

export function setActorSearchActorsData(
  client: QueryClient,
  queryKeyBase: [
    parameters: {
      q?: string | undefined;
      limit?: number | undefined;
      cursor?: string | undefined;
    },
  ],
  data: operations.AppBskyActorSearchActorsResponse,
): operations.AppBskyActorSearchActorsResponse | undefined {
  const key = queryKeyActorSearchActors(...queryKeyBase);

  return client.setQueryData<operations.AppBskyActorSearchActorsResponse>(
    key,
    data,
  );
}

export function invalidateActorSearchActors(
  client: QueryClient,
  queryKeyBase: TupleToPrefixes<
    [parameters: {
      q?: string | undefined;
      limit?: number | undefined;
      cursor?: string | undefined;
    }]
  >,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "actor", "searchActors", ...queryKeyBase],
  });
}

export function invalidateAllActorSearchActors(
  client: QueryClient,
  filters?: Omit<InvalidateQueryFilters, "queryKey" | "predicate" | "exact">,
): Promise<void> {
  return client.invalidateQueries({
    ...filters,
    queryKey: ["bluesky", "actor", "searchActors"],
  });
}

export function buildActorSearchActorsQuery(
  client$: BlueskyCore,
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext,
  ) => Promise<operations.AppBskyActorSearchActorsResponse>;
} {
  return {
    retry: false,
    queryKey: queryKeyActorSearchActors({
      q: request?.q,
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function actorSearchActorsQuery(
      ctx,
    ): Promise<operations.AppBskyActorSearchActorsResponse> {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      return unwrapAsync(actorSearchActors(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}

export function buildActorSearchActorsInfiniteQuery(
  client$: BlueskyCore,
  request?: operations.AppBskyActorSearchActorsRequest | undefined,
  options?: RequestOptions,
): {
  retry: boolean;
  queryKey: QueryKey;
  queryFn: (
    context: QueryFunctionContext<QueryKey, ActorSearchActorsPageParams>,
  ) => Promise<
    PageIterator<
      operations.AppBskyActorSearchActorsResponse,
      { cursor: string }
    >
  >;
} {
  return {
    retry: false,
    queryKey: queryKeyActorSearchActors({
      q: request?.q,
      limit: request?.limit,
      cursor: request?.cursor,
    }),
    queryFn: async function actorSearchActorsQuery(
      ctx,
    ): Promise<
      PageIterator<
        operations.AppBskyActorSearchActorsResponse,
        { cursor: string }
      >
    > {
      const sig = combineSignals(ctx.signal, options?.fetchOptions?.signal);
      const mergedOptions = {
        ...options,
        fetchOptions: { ...options?.fetchOptions, signal: sig },
      };

      if (!ctx.pageParam) {
        return unwrapResultIterator(actorSearchActors(
          client$,
          request,
          mergedOptions,
        ));
      }

      return unwrapResultIterator(actorSearchActors(
        client$,
        {
          ...request,
          cursor: ctx.pageParam.cursor,
        },
        mergedOptions,
      ));
    },
  };
}

export function queryKeyActorSearchActors(
  parameters: {
    q?: string | undefined;
    limit?: number | undefined;
    cursor?: string | undefined;
  },
): QueryKey {
  return ["bluesky", "actor", "searchActors", parameters];
}
